# Cognito認証情報
TEST_USER_EMAIL=your-test-user@example.com
TEST_USER_PASSWORD=your-test-user-password

# テスト設定
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR, CRITICAL
WEBSOCKET_TIMEOUT=600  # WebSocket接続のタイムアウト（秒）
MAX_RETRY_COUNT=3  # 再試行回数

# 注意事項：
# 1. このファイルを.envにコピーしてください
# 2. 各値を実際の値に置き換えてください
# 3. .envファイルはGitにコミットしないでください（.gitignoreに追加済み）
# 4. TEST_USER_PASSWORDは以下の要件を満たす必要があります：
#    - 8文字以上
#    - 大文字を含む
#    - 小文字を含む
#    - 数字を含む
#    - 特殊文字を含む