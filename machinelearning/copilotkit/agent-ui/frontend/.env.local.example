# Cognito Configuration
# CloudFormationスタックから自動取得するか、手動で設定してください
COGNITO_CLIENT_ID=your-cognito-client-id
COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
# 以下のコマンドで生成: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl

# AWS Configuration
AWS_REGION=us-east-1

# 使用方法:
# 1. このファイルを .env.local にコピー
#    cp .env.local.example .env.local
#
# 2. 自動セットアップスクリプトを使用（推奨）
#    ./scripts/setup-and-run.sh
#
# 3. または手動で各値を設定
#    - COGNITO_CLIENT_ID: CloudFormationスタックの出力から取得
#    - COGNITO_ISSUER: CloudFormationスタックの出力から取得
#    - NEXTAUTH_SECRET: openssl rand -base64 32 で生成
