# CopilotKit Agent UI - 本番環境設定

# NextAuth.js Secret (本番用 - 必ず変更してください!)
# 以下のコマンドで生成: openssl rand -base64 32
NEXTAUTH_SECRET=

# AWS Settings
AWS_REGION=us-east-1
CDK_DEFAULT_REGION=us-east-1

# Cognito Client Suffix (SSMパラメータのパスに使用)、環境ごとに変えてください。
COGNITO_CLIENT_SUFFIX=copilotkit-example

# フロントエンドディレクトリ選択
DEPLOY_FRONTEND_DIR=frontend

# 本番環境設定
ENVIRONMENT=example
DEBUG_MODE=true
# NEXTAUTH_URLはCDKデプロイ時にCloudFront URLで自動設定される

# Optional: AWS Profile
# AWS_PROFILE=default
