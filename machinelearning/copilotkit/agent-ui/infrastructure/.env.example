# CopilotKit Agent UI - 環境変数設定
# このファイルをコピーして .env を作成し、実際の値を設定してください
# 
# 注意: setup.shスクリプトを実行すると自動的に.envファイルが作成されます

# NextAuth.js Secret (以下のコマンドで生成: openssl rand -base64 32)
# setup.shスクリプトで自動生成されます
NEXTAUTH_SECRET=

# AWS Settings (必要に応じて変更)
AWS_REGION=us-east-1
CDK_DEFAULT_REGION=us-east-1

# Optional: AWS Profile (複数のAWSアカウントを使用する場合)
# AWS_PROFILE=default

# ========================================
# 重要: 以下の変数は不要になりました
# ========================================
# COGNITO_CLIENT_SECRET: Public Client設定のため不要
# Cognito設定はCDKで自動管理されます
