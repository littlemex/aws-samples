# Stack configuration
STACK_NAME=MyEc2SsmStack                # Optional: Stack name (default: Ec2SsmCdkStack-[timestamp])
VPC_NAME=MyCustomVPC                    # Optional: VPC name (default: VPC)

# EC2 Instance configuration
EC2_INSTANCE_CLASS=g5                   # Optional: EC2 instance class (default: g6)
EC2_INSTANCE_SIZE=xlarge8              # Optional: EC2 instance size (default: xlarge4)

AMI_NAME="Deep Learning OSS Nvidia Driver AMI GPU PyTorch 2.5*Ubuntu*"

# AWS Region (usually set by CDK)
CDK_DEFAULT_REGION=us-east-1           # Optional: AWS region (default: us-east-1)

# Example usage:
# 1. Copy this file to .env:
#    cp .env.sample .env
# 
# 2. Modify the values in .env as needed
#
# 3. Deploy with:
#    source .env && cdk deploy
